type Card {
  wordValue: String
  typeValue: CardType
  position: Position
  isRevealed: Boolean
}

enum CardType {
  RED_AGENT
  BLUE_AGENT
  BYSTANDER
  ASSASSIN
}

type Clue {
  word: String
  number: Int
  reasoning: String
}

type Game {
  board: [Card]
  turn: Team
  currentClue: Clue
}

type GuessCard {
  ok: Boolean
}

type InitializeGame {
  game: Game
}

type Mutation {
  guessCard(position: PositionInput!): GuessCard
  initializeGame: InitializeGame
}

type Position {
  x: Int
  y: Int
}

input PositionInput {
  x: Int
  y: Int
}

type Query {
  card(position: PositionInput!): Card
  game: Game
}

enum Team {
  RED
  BLUE
}
